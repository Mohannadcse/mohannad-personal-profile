# uses prebuilt image from dockerhub
services:
  jekyll:
    image: amirpourmand/al-folio:slim
    platform: linux/amd64
    ports:
      - 8080:8080
      - 35729:35729
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
